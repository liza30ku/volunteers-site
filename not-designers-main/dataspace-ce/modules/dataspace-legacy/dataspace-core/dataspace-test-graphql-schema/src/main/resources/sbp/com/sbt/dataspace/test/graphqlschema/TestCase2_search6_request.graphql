{
    searchProduct(cond: "root.$id == '${product1Id}'") {
        elems {
            id
            services @skip(if: false) {
                elems {
                    id
                }
            }
        }
    }
}